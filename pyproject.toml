[build-system]
requires = ["setuptools >= 62.3.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 130

[tool.ruff]
line-length = 130
ignore = ["E501"]
include = ["snowkill/*"]

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"]
